#include <cstdio>
#include <iostream>
#include <cstdlib>
#include <ctime>

using namespace std;



int main() {

   // freopen("test1.txt", "w", stdout);
    srand(time(NULL));
    long long a, b;
    a = rand() % 10000000000000000;
    b = rand() % 10000000000000000;
    cout << a << endl << b << endl;
   // freopen("test1.gold", "w", stdout);
    cout << a * b << endl;
}
